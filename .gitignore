# Unity Project Generated Files #
#################################

/[Uu]ser[Ss]ettings/
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Bb]uild/
/[Bb]uilds/
/[Ll]ogs/
/[Mm]emoryCaptures/
/[Rr]ecordings/
/[Uu]ser[Ss]ettings/ # Optional: Exclude UserSettings folder

# MemoryCaptures can be huge. If you want memory captures to be shared, remove the line above

# Asset meta Bumps - Not required in source control #
#####################################################
# Uncomment if you are having problems merging assetbundles / addresses #
# /[Aa]ssets/[Aa]ddressable[Aa]ssets[Dd]ata/*.*.meta

# AssetStoreTools caches #
#------------------------#
/[Aa]sset[Ss]tore*

# Visual Studio / Rider temporary files #
#---------------------------------------#
.vs/ # Visual Studio IDE temporary files
*.csproj
*.sln.user
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# Unity3D generated meta files #
#------------------------------#
# Necessary line endings and files based on the Unity documentation:
# https://docs.unity3d.com/Manual/UsingGIT.html
# Instead of removing those meta files from Git, we are using a pattern
# matching the beginning of files or folders starting with a dot.
# Attention: This is specific to file or folders starting with a single dot.
# If you are using something like .github folder, you need to create another entry to ignore it
# or if you are using multiple layers of folders starting with a dot, you might need to create
# specific rules for it.
# Examples:
# .vs            - suppressed by the rule below
# .github        - not suppressed by the rule below
# .vscode        - not suppressed by the rule below
# node_modules   - not suppressed by the rule below
# a/.folder      - not suppressed by the rule below
.[^.]*/
!.github/

# Builds #
#--------#
*.apk
*.unitypackage

# CrashReporter logs on Mac #
#---------------------------#
# These files can be big and are specific to the crash context
# You might want to track the reports instead:
# https://docs.unity3d.com/Manual/LogFiles.html
sysinfo.txt
crash.*.txt
*_BurstDebugInformation_DoNotShip/

# Visual Studio Code #
#--------------------#
.vscode*/ # Does not suppress .vscode folder above Unity project root

# Rider #
#-------#
.idea*/ # Does not suppress .idea folder above Unity project root

# Autogenerated FMOD files (if using FMOD) #
#------------------------------------------#
/[Aa]ssets/Plugins/FMOD/Cache/*
/[Aa]ssets/Plugins/FMOD/Documentation.url
/[Aa]ssets/Plugins/FMOD/FMOD Demo.* # Example Scenes
/[Aa]ssets/Plugins/FMOD/*.user

# JetBrains Rider Tools #
#-----------------------#
# Covers files like ProjectSettings/Packages/rider.project.model.xml rider Sln / Csproj support generation
# Warning: Including this requires Rider to re-generate these files on checkout, possibly heavy on large projects
/[Pp]roject[Ss]ettings/[Pp]ackages/rider.*
/[Rr]ider/

# PlasticSCM generated files #
#----------------------------#
# It is recommended that PlasticSCM is not used alongside Git. These rules ensure
# the PlasticSCM working copy remains pristine for specific files (.plasticignore for instance).
# Covers the primary file for PlasticSCM, ensuring the checkout can remain usable with that system
.plasticignore
# Covers files and folders related to PlasticSCM when the plugin is enabled
.plastic/*
/[Aa]ssets/Plugins/PlasticSCM*

# Packages Cache#
#------------------------------------------#
# Usually huge, can be rebuilt easily
/[Ll]ibrary/[Pp]ackage[Cc]ache/